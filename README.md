# synology_nginx_config
a few config of nginx  on a synology

config files locate at a synology:
/usr/local/nginx/conf.d/
after edit a config, you need restart nginx with command:
synoservice --restart nginx
